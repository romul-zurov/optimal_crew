program optimal_crew_porj;

uses
  Forms,
  optimal_crew in 'optimal_crew.pas' {form_main};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(Tform_main, form_main);
  Application.Run;
end.
